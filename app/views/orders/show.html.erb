<script src="//code.jquery.com/jquery-1.11.1.min.js"></script>
<!------ Include the above in your HEAD tag ---------->
<div class="container m-5">
  <div class="row">
    <div class="well col-xs-10 col-sm-10 col-md-6 col-xs-offset-1 col-sm-offset-1 col-md-offset-3">
      <div class="row">
        <div class="text-center">
          <h1>Ton reçu mon Castor !</h1>
        </div>
      </span>
      <table class="table table-hover">
        <thead>
          <tr>
            <th>Produirs</th>
            <th>Quantité</th>
            <th class="text-center">Prix</th>
            <th class="text-center">Total</th>
          </tr>
        </thead>
        <tbody>
          <tr>
            <% @orderItems.each do |orderitem|  %>
              <td class="col-md-9"><em><%= orderitem.get_item.title %> </em></h4>
            </td>
            <td class="col-md-1" style="text-align: center"> <%= orderitem.quantity %>  </td>
            <td class="col-md-1 text-center"> <%= number_to_euro(orderitem.get_item.price) %></td>
            <td class="col-md-1 text-center"><%= number_to_euro(orderitem.get_price) %></td>
          </tr>
        <% end %>
        <tr>
          <tr>
            <td>   </td>
            <td>   </td>
            <td class="text-right">
              <h4><strong>Total: </strong></h4>
            </td>
            <td class="text-center text-danger">
              <h4><strong><%= number_to_euro(@order.get_price) %></strong></h4>
            </td>
          </tr>
        </tbody>
      </table>
    </td>
  </div>
</div>
</div>