$(document).ready(function(){
  $("#cart-item-total-price").text("<%= number_to_euro(@cart.get_price)%>");
  $("#quantity-<%=@cart_item.id%>").text("<%=@cart_item.quantity%>");
  $("#item-total-<%=@cart_item.id%>").text("<%=number_to_euro(@cart_item.get_price)%>");
});